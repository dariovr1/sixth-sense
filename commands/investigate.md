---
description: Run Dylan Dog directly — premortem on a spec/design, or edge-case hunting on a diff.
---
Use the `Skill` tool with `skill: "sixth-sense:dylan-dog"` (or, on Antigravity, `activate_skill` with `dylan-dog`) to load and run it — use this exact qualified name; a bare "dylan-dog" would collide with this command's own name if invoked unqualified. Ask the user (if not already obvious from context) whether this is Premortem mode (on a spec/design that hasn't been approved yet) or Hunting mode (on a code diff), then follow that skill's process for the chosen mode.
